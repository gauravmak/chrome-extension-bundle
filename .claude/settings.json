{
  "$comment": "Project-level Claude Code settings for the SuperLevels Chrome extension. The Stop hook runs scripts/check-instrumentation.mjs at the end of every Claude response. If gaps are found (handlers missing logs or popup.js handlers missing notifications), it exits non-zero which feeds the script output back into Claude's context as a blocking error, so Claude is prompted to fix the gap before declaring the turn done.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && node scripts/check-instrumentation.mjs 1>&2 && node scripts/check-security.mjs 1>&2"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(node scripts/check-instrumentation.mjs)",
      "Bash(node scripts/check-security.mjs)",
      "Bash(node --check *)"
    ]
  }
}
